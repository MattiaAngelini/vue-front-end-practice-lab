export class Button {
    label:string;
    url: string;
    bgColor: string;
    color: string

    constructor(label:string,url:string, bgColor:string, color: string){
        this.label = label
        this.url = url
        this.bgColor = bgColor
        this.color = color
    }

  }