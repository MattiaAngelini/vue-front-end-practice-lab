export class SideBySide {
    title: string;
    description: string;
    image: string;
    urlButton: string;
  
    constructor(title: string, description: string, image: string, urlButton:string) {
      this.title = title;
      this.description = description;
      this.image = image;
      this.urlButton = urlButton;
    }
  }